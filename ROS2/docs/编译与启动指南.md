# ROS2 gmapping 编译与启动指南

> 适用于 `osrf/ros:humble-desktop-full` 镜像

---

## 一、快速开始（推荐 - 使用管理脚本）

### 1. 配置代理（如使用科学上网）

编辑 `ros2_docker.sh`，修改代理地址为你的实际值：

```bash
HTTP_PROXY="${HTTP_PROXY:-http://127.0.0.1:7897}"
HTTPS_PROXY="${HTTPS_PROXY:-http://127.0.0.1:7897}"
```

### 2. 启动容器

```bash
cd /path/to/slam_gmapping/ROS2
chmod +x ros2_docker.sh
./ros2_docker.sh start
```

### 3. 安装依赖

```bash
./ros2_docker.sh deps
```

脚本会自动：
- 通过代理安装 apt 系统依赖
- 克隆 `openslam_gmapping` 源码到 `src/` 目录

### 4. 编译

```bash
./ros2_docker.sh build
```

### 5. 进入容器

```bash
./ros2_docker.sh shell
```

> **说明**：本地项目目录已通过 Docker 卷自动映射到容器内，修改本地文件即同步到容器。

---

## 二、手动编译（传统方式）

### 1. 启动容器并映射本地项目目录

```bash
cd /path/to/slam_gmapping/ROS2
docker run -d \
  --name ros2-gmapping \
  --privileged \
  --network host \
  -e HTTP_PROXY=http://127.0.0.1:7897 \
  -e HTTPS_PROXY=http://127.0.0.1:7897 \
  -e http_proxy=http://127.0.0.1:7897 \
  -e https_proxy=http://127.0.0.1:7897 \
  -e DISPLAY=$DISPLAY \
  -e QT_X11_NO_MITSHM=1 \
  -v /tmp/.X11-unix:/tmp/.X11-unix \
  -v $(pwd):/root/ros2_ws/src \
  -v ~/.ssh:/root/.ssh:ro \
  -w /root/ros2_ws \
  osrf/ros:humble-desktop-full \
  tail -f /dev/null

# 配置 apt 使用代理
docker exec ros2-gmapping bash -c "echo 'Acquire::http::Proxy \"http://127.0.0.1:7897\";' > /etc/apt/apt.conf.d/99custom"
```

### 2. 进入容器

```bash
docker exec -it ros2-gmapping bash -c "source /opt/ros/humble/setup.bash && bash"
```

### 3. 安装系统依赖

```bash
# 如果使用了代理，需要设置环境变量
export http_proxy=http://127.0.0.1:7897
export https_proxy=http://127.0.0.1:7897

apt-get update
apt-get install -y \
  ros-humble-nav2-bringup \
  ros-humble-teleop-twist-keyboard \
  ros-humble-slam-toolbox \
  python3-colcon-common-extensions \
  ros-humble-rclcpp-components \
  ros-humble-nav2-msgs \
  ros-humble-rosbag2-cpp \
  ros-humble-rosbag2-storage
```

> **注意**：`ros-humble-openslam-gmapping` 不在 apt 仓库中，需要从源码编译。

### 4. 克隆 openslam_gmapping 源码

```bash
cd /root/ros2_ws/src
git clone https://github.com/ros-perception/openslam_gmapping.git
```

> **重要**：该仓库只有 `melodic-devel`（ROS1）分支，但它是纯 C++ 库，编译到 ROS2 完全没问题。项目已自带适配 ament_cmake 的 `CMakeLists.txt`。

### 5. 编译

```bash
cd /root/ros2_ws
source /opt/ros/humble/setup.bash

# 编译 openslam_gmapping（已配置 -fPIC）
colcon build --packages-select openslam_gmapping --cmake-args -DCMAKE_BUILD_TYPE=Release

# 编译 gmapping
colcon build --packages-select gmapping --cmake-args -DCMAKE_BUILD_TYPE=Release

# 或一次性编译
colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release
```

### 6. 加载环境

```bash
source install/setup.bash
```

---

## 三、启动 - 完整 SLAM 栈（推荐测试）

这是**最简单的测试方式**，一键启动所有需要的组件：

```bash
# 宿主机执行
./ros2_docker.sh build
./ros2_docker.sh complete_slam
```

这会启动：
1. **robot_state_publisher** — 发布 `base_link→laser_frame` TF
2. **odometry_publisher** — 订阅 `/cmd_vel`，发布 `odom→base_link` TF（模拟里程计）
3. **gmapping** — 接收 `/scan` + 里程计，构建地图

然后在另一个终端启动键盘遥控：

```bash
./ros2_docker.sh shell
source /root/ros2_ws/install/setup.bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

按 `i`（前进）、`,`（后退）、`j`（左转）、`l`（右转）控制机器人移动建图。

### 查看地图（可选）

```bash
# 宿主机执行
xhost +local:docker

# 容器内执行
source /opt/ros/humble/setup.bash
rviz2
```

在 rviz2 中添加 **Map** 显示，Topic 选择 `/map`。

### 建图完成后保存地图

```bash
source /root/ros2_ws/install/setup.bash
ros2 run nav2_map_server map_saver_cli -f ~/my_map
```

---

## 四、启动 - 手动遥控建图模式（需要真实硬件）

> 此模式需要连接激光雷达和底盘，适用于真实机器人。

### 终端 1：启动激光雷达 + gmapping

```bash
# 启动激光雷达驱动
ros2 launch laser_driver laser_driver.launch.py

# 另一个终端启动 gmapping
source /root/ros2_ws/install/setup.bash
ros2 launch gmapping slam_gmapping.launch.py \
  scan_topic:=/scan \
  base_frame:=base_link \
  odom_frame:=odom \
  map_frame:=map
```

### 终端 2：启动底盘（需要底盘驱动节点）

```bash
# 你的底盘驱动
ros2 run your_robot_driver your_driver_node
```

### 终端 3：启动键盘遥控

```bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

---

## 四、启动 - 自主探索建图模式

> 需要额外安装 `explore_lite`

### 安装 explore_lite

```bash
cd /root/ros2_ws/src
git clone https://github.com/hrnjcl/explore_lite.git
cd /root/ros2_ws
colcon build --packages-select explore_lite
source install/setup.bash
```

### 启动自主探索建图

**终端 1**：启动底盘驱动 + gmapping（同上）

**终端 2**：启动 explore_lite

```bash
source /root/ros2_ws/install/setup.bash
ros2 run explore_lite explore
```

机器人会自动探索环境并建图，完成后保存：

```bash
ros2 run nav2_map_server map_saver_cli -f ~/my_map
```

---

## 五、使用 Navigation2 完整导航栈

建图完成后，使用 Nav2 进行导航：

### 1. 启动 Nav2（使用已保存的地图）

```bash
source /opt/ros/humble/setup.bash
ros2 launch nav2_bringup navigation_launch.py \
  use_sim_time:=false \
  map:=~/my_map.yaml
```

### 2. 在 rviz2 中使用 "Nav2 Goal" 工具点击目标位置进行导航

---

## 六、回放 rosbag2 测试

使用已有的 rosbag2 数据测试 gmapping：

```bash
source /root/ros2_ws/install/setup.bash
ros2 run gmapping slam_gmapping_replay --bag_filename /path/to/bag.db3
```

---

## 七、作为 Component 加载

```bash
# 启动 component container
source /root/ros2_ws/install/setup.bash
ros2 run rclcpp_components component_container

# 加载 gmapping component
ros2 component load /ComponentManager gmapping SlamGMappingComponent
```

---

## 八、自定义 launch 参数

```bash
ros2 launch gmapping slam_gmapping.launch.py \
  scan_topic:=/scan \
  base_frame:=base_link \
  odom_frame:=odom \
  map_frame:=map \
  map_update_interval:=5.0 \
  particles:=30 \
  delta:=0.05 \
  max_urange:=16.0
```

完整参数列表见 `项目架构与能力说明.md`。

---

## 九、管理脚本命令速查

```bash
./ros2_docker.sh start           # 启动容器
./ros2_docker.sh stop            # 停止容器
./ros2_docker.sh rm              # 删除容器
./ros2_docker.sh shell           # 进入容器 bash
./ros2_docker.sh deps            # 安装系统依赖 + 克隆 openslam_gmapping
./ros2_docker.sh build           # 编译所有包
./ros2_docker.sh gmapping        # 启动 gmapping 建图（需要外部 /scan 和 odom TF）
./ros2_docker.sh complete_slam   # 启动完整 SLAM 栈（含模拟里程计 + gmapping）
./ros2_docker.sh teleop          # 启动键盘遥控
./ros2_docker.sh rviz2           # 启动 RViz2
./ros2_docker.sh save-map        # 保存地图
./ros2_docker.sh replay          # 回放 rosbag 测试
```

## 十、项目结构说明

```
ROS2/
├── gmapping/                      # SLAM 算法包（ROS2 适配）
│   ├── src/                       # 核心代码
│   └── launch/
│       ├── slam_gmapping.launch.py
│       └── complete_slam.launch.py  # 一键启动完整 SLAM
│
├── laser_driver/                  # 激光雷达驱动包（Camsense X2M）
│   ├── src/laser_driver_node.cpp
│   └── launch/laser_driver.launch.py
│
├── odometry_publisher/            # 模拟里程计发布包
│   └── src/odometry_publisher.cpp
│
├── robot_state_publisher_pkg/     # 机器人状态发布包
│   ├── urdf/robot.urdf
│   └── launch/robot_state_publisher.launch.py
│
├── openslam_gmapping/             # OpenSLAM GMapping 库（源码编译）
│
└── ros2_docker.sh                 # 容器管理脚本
```

---

## 十、常见问题

### Q: apt-get 报错 502 Bad Gateway 或 NOSPLIT

通常是因为科学上网代理不稳定。解决方法：

1. **关闭代理直连**（容器使用 `--network host` 时可能不需要代理）：
   ```bash
   docker exec ros2-gmapping bash -c "http_proxy= https_proxy= apt-get update"
   ```

2. **重试安装**（脚本已自动重试 3 次）：
   ```bash
   ./ros2_docker.sh deps
   ```

3. **修复半安装状态**：
   ```bash
   docker exec ros2-gmapping bash -c "apt --fix-broken install -y"
   ```

### Q: 编译报错找不到 openslam_gmapping

`openslam_gmapping` 不在 apt 仓库中，需要从源码编译：

```bash
cd /root/ros2_ws/src
git clone https://github.com/ros-perception/openslam_gmapping.git
cd /root/ros2_ws
colcon build --packages-select openslam_gmapping --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
colcon build --packages-select gmapping --cmake-args -DCMAKE_BUILD_TYPE=Release
```

> **注意**：该仓库没有 `ros2` 分支，使用默认的 `melodic-devel` 即可（纯 C++ 库，无 ROS 依赖）。

### Q: 编译 gmapping 报 `recompile with -fPIC` 错误

`openslam_gmapping` 编译时需要 `-fPIC` 选项才能链接到共享库。项目已配置此选项，如果手动修改过 `CMakeLists.txt`，请确保包含：

```cmake
add_compile_options(-fPIC)
```

修复后需要清理并重新编译：

```bash
rm -rf build/openslam_gmapping install/openslam_gmapping
colcon build --packages-select openslam_gmapping gmapping --cmake-args -DCMAKE_BUILD_TYPE=Release
```

### Q: 启动后收不到 /scan 数据

检查话题名是否匹配，通过 launch 文件的 `scan_topic` 参数修改：

```bash
ros2 launch gmapping slam_gmapping.launch.py scan_topic:=your_scan_topic
```

### Q: TF 变换树不完整

确保 `robot_state_publisher` 正确加载了 URDF，发布了 `base_link`→`laser_frame`。

### Q: 地图不更新

检查 `linearUpdate`/`angularUpdate` 参数，确保机器人在移动。

### Q: 地图保存后找不到

确保 `nav2_map_server` 已安装：

```bash
apt-get install ros-humble-nav2-map-server
```

### Q: launch 文件报错 `NameError: name 'str_' is not defined`

这是 launch 文件中 `str` 变量与 Python 内置函数冲突的问题。项目已修复此问题，如果拉取了旧版本代码，请确保 `launch/slam_gmapping.launch.py` 中使用的是 `str_` 而非 `str`。

### Q: 容器内 GUI 程序（rviz2）无法显示

确保宿主机执行了 `xhost +local:docker`，并且容器启动时挂载了 `/tmp/.X11-unix`。
